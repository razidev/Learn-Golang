<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{{.Title}}</title>
    </head>
    <body>
        {{/*ini komentar*/}}
        <h1>Name: {{.Name}}</h1>
        {{with .Address}}
            <h1>Street: {{.Street}}</h1>
            <h1>City: {{.City}}</h1>
        {{else}}
            <h1>Anda tidak punya alamat</h1>
        {{end}}
    </body>
</html>